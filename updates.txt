## [2026-03-18] Beginner Support & QoL (builds 111–112)

### Pause Game (build 112)
- Press **SPACE** to pause/resume during gameplay
- Dark overlay with `PAUSED` text and resume hint displayed while paused
- All game logic halted; UI remains visible; resets on restart/continue

### Elite Enemy Drop Change (build 111)
- Elite and Dark Skull enemies now drop a **spice gem** on death instead of a pickup
- Spice amount: `random(1 ~ wave+10)` (same as regular spice spawns)
- Kill feed updated: `+ PICKUP DROP` → `+ SPICE DROP`

### Dark Skull Mist — Radius Reduction (build 111)
- Mist radius reduced **−10%** (432 → 389 world units)

### Pickup & Spice Limits (build 113)
- `MAX_PICKUPS` raised 7 → 10
- `MAX_SPICES` raised 6 → 10
- Elite spice drops now respect the `MAX_SPICES` cap

### Beginner Wave Extension (build 111)
- Wave 4+: if **10 or more enemies** are on screen at the wave boundary, the next wave is delayed
- Extension = **5s × (wave − 3)** (wave 4: +5s, wave 5: +10s, wave 6: +15s, …)
- Kill feed shows `⏳ WAVE EXTENDED +Xs — clear enemies!` when triggered

---

## [2026-03-18] Balancing & Bug Fixes (builds 108–110)

### Fever Mode — Visual Overhaul (build 108)
- **Screen-edge fire vignette**: orange glow burns around entire screen border during fever
- **Full-screen orange tint**: subtle warm overlay covers the screen while active
- **`🔥 FEVER Xs` countdown**: text displayed top-center; grows larger and pulses red in final 3s warning phase
- Larger pulsing aura behind player (radius 56 → 90)
- Player character brightened during fever: `hsl(22,100%,68%)` → `hsl(22,100%,75%)`

### Fever Dash [D] — Duplicate Activation Fix (build 108)
- Activating [D] while fever is already running now **extends the timer +10s** instead of refunding
- Previously caused an infinite freeze loop (`while` refund cycle); fixed

### MINIMEE [N] — Infinite Loop Fix (build 108)
- Fixed freeze when MINIMEE was at max (3/3) and spice kept accumulating: added `break` to exit the while loop

### Elite Enemy — Balancing (builds 108–110)
- Spawn rate reduced: **5% → 3%** (white skull), **~5% → ~3%** (dark skull, threshold 10% → 6%)
- HP formula changed: `baseHp * 3` → `5 + baseHp * 3`
- Elite enemies now **clamped to arena boundary** on spawn (can no longer root outside `ARENA_R`)
- Seize tremor slowed **−20%** (0.027→0.0216, 0.037→0.0296) after previous −67% (was 0.08/0.11)

### Dark Skull — Mist Tuning (build 110)
- Mist radius reduced **−10%** (480 → 432 world units)
- Mist opacity brightened **+10%** (core 1.0→0.90, mid 0.97→0.87, edge 0.70→0.63)

### Minor
- Leaderboard decay notice: "−1% every day" → "1% every day"

---

## [2026-03-18] Elite Enemy, Split Shot, Fever Dash (builds 103–107)

### Dark Skull Elite Variant (build 106)
- Second elite type: **5% chance** (separate roll after white skull check, ~5% additional)
- `darkElite: true` flag — same seize/root/3×HP/guaranteed-pickup as white skull
- Visual: very dark body (`#1a0033`), deep violet halo (`#7700cc` / purple glow), purple `☠` skull overlay
- On spawn: float text `☠ DARK SKULL` in violet
- **Dark Mist**: activates **3 seconds after spawn** (`DARK_MIST_R = 220` world units)
  - Deep dark radial gradient (near-black core fading to transparent) drawn over the world
  - **Any enemy inside the mist is hidden** — skipped entirely from the draw loop
  - The dark skull itself remains visible (exempt from hiding logic)
  - Mist fades in over 1.2s for a creeping appearance; pulses gently while active
- Kill feed: `☠ DARK SKULL DOWN! +N + PICKUP DROP`
- Also fixes a bug: `continue` inside `enemies.forEach` (SyntaxError) corrected to `return`

### Elite Enemy — Skull Variant (build 105)
- **5% chance** any enemy spawns as an elite on `spawnEnemy()`
- Elite properties: **3× HP**, **26px radius** (vs 22), stays rooted at spawn position
- "Seizing" animation: rapid Lissajous tremor (~13–17 Hz offset on screen coords)
- Visual: silver/white body (`#dddddd`), glowing gold halo ring, `☠` skull overlay
- On spawn: float text `☠ ELITE` appears
- **Guaranteed pickup drop** on death — all kill paths covered: bullet, frozen, fever, EXTRA board, mine blast
- Kill feed: `☠ ELITE DOWN! +N + PICKUP DROP`
- Elite enemies skip the awakened mechanic

### Split Shot [SPL] — New Pickup (build 104)
- New pink pickup (`#ff66bb`), label `SPL`, name "Split Shot"
- Activation rate: **5% at level 1, +2% per level** (formula: `(5 + (upg.split-1)*2) / 100`)
- When a split-flagged bullet hits an enemy or boss, it spawns **3 child bullets** spreading at −60°, 0°, +60° from the impact direction
- Child bullets (`splitChild: true`) do not split again — no chain recursion
- Split bullet visually pink (`#ffbbdd` fill / `#ff66bb` outline); child bullets same color
- `upg.split` starts at 1, caps at 10 (max 23% chance); resets on restart
- Appears in upgrade status panel (SPL row, pink)

### Fever Dash [D] — Spice Product (build 103)
- New spice product type `d` — Fever Dash, symbol `[D]`
- On activation: player enters **FEVER MODE** for 10 seconds (`FEVER_DURATION = 600` frames)
- Fever mode effects:
  - Any enemy that touches the player is instantly destroyed (explosion + score bonus)
  - All incoming enemy bullets are absorbed on contact — no damage taken
  - Player character glows with a hot orange radial aura (`#ff6600` shadow + gradient)
- Warning: final 3 seconds (`FEVER_WARN_AT = 180`), player aura blinks at ~5 Hz to signal mode ending
- Activating again while already in fever refunds the cost and shows a feed notice
- HUD: `[M][N][I][D]` selector row — `[D]` button highlights orange when active
- Key `1`/`E` cycles through all four products

---

## [2026-03-17] Rainbow EXTRA, Ad Removal & Score Decay (builds 101–102)

### EXTRA Letter — Rainbow Colors (builds 101–102)
- Each EXTRA board letter now glows in its own color when lit: E=red, X=orange, T=yellow, R=green, A=violet
- Ground pickup orbs match the same per-letter color: ring, shadow, gradient fill, and border all use the letter's color
- `EXTRA_COLORS = { e:'#ff3333', x:'#ff8800', t:'#ffee00', r:'#33cc55', a:'#aa44ff' }` map added
- Collection explosion particles and float text also use the letter's color (was always gold `#ffcc00`)

### Ad System Removed (build 101)
- Removed Google AdSense `<script>` tag from `index.html`
- Removed "CONTINUE AFTER AD" button and entire `#ad-overlay` div
- Removed `showAdOverlay()`, `continueFromAd()`, `adUsed`, `adCountdownInterval` from `game.js`
- Removed all ad-related CSS (`#ad-overlay`, `#ad-card`, `#ad-container`, `#continue-ad-btn`, etc.)

### Leaderboard — Daily Score Decay (build 101)
- All leaderboard scores decrease by **−1% per day** (floor, minimum 1)
- Triggered server-side on every leaderboard GET request
- `meta` table added to SQLite DB; `last_process_date` checked — decay runs at most once per calendar day
- Game over screen shows notice: `⚠ Leaderboard scores decay −1% every day`

---

## [2026-03-16] Heart Gauge Redesign (builds 99–100)

### Heart Gauge — 7 Slots, Two Golden on Left (build 100)
- Total heart slots expanded **6 → 7**
- Layout left→right: `♥(gold·h7) ♥(gold·h6) | ♥h5 ♥h4 ♥h3 ♥h2 ♥h1`
- Two golden slots (h6, h7) sit on the far left — dark at game start
- Each EXTRA completion lights up one golden slot (h6 first, then h7)
- h5 promoted to a regular red heart slot (was the only golden slot before)
- h0 removed; max life raised **6 → 7**; EXTRA cap updated accordingly
- `_dom.hearts` array now maps `[h1, h2, h3, h4, h5, h6, h7]` (h1 = first to die, rightmost)

### Heart Gauge — Order Fix (build 99)
- Golden heart slot moved from leftmost to rightmost position

---

## [2026-03-16] Label Renames, First Pickup & Sprout Limit (builds 96–98)

### Sprout/Tree Max Limit Raised (build 96)
- Max combined sprouts + trees raised **8 → 10** (+25%)
- Barrier walls now appear more frequently on the field

### First Pickup Always Move Speed (build 97)
- The very first pickup each game is guaranteed to be **Move Speed (SPD)**
- `firstPickupDone` flag resets on restart/continue; subsequent pickups use normal random pool

### Label Renames (builds 97–98)
- Move Speed label: `MOV` → `SPD` (pickup orb + upgrade status panel)
- Fire Rate label: `RTE` → `RLD` (reload — pickup orb + upgrade status panel)

---

## [2026-03-16] Barrier Lifetime, EXTRA Wave Fix (builds 94–95)

### Barrier — 60s Lifetime from Spawn (build 94)
- `barrierTimer` now initialized to `BARRIER_WALL_LIFE` (3600 frames = 60s) at **spawn**, not only when activated
- Timer counts down while the sprout is still growing — barrier disappears if never fully grown within 60s
- When promoted to full wall, timer continues from remaining time (not reset)
- Full wall expiry message: "Barrier gone"; growing sprout expiry: "Sprout gone"

### EXTRA — No Spawn on Wave 1 (build 95)
- EXTRA letters now only drop from **wave 2+** (`waveNow > 1 && waveNow > lastExtraWave`)
- Prevents a letter from appearing immediately at game start

---

## [2026-03-16] EXTRA Polish, Golden Heart & Barrier Tree (builds 90–93)

### EXTRA Letter System — Spawn Rework (build 93)
- Letters no longer spawn on a timer; one random letter drops each time the **wave increases**
- Letter is picked from all 5 (E/X/T/R/A) — duplicates and already-collected letters can appear
- `lastExtraWave` tracks the last wave a letter was dropped; resets on restart/continue

### EXTRA Letter System — Visual (build 91)
- Pickup shape changed from circle to **rounded square**
- Gold linear gradient fill (`#fff9cc` → `#ffcc00` → `#ff8800`), amber border, pulsing square ring outline

### Heart System Rework (build 92)
- **Heart pickup removed** from the pickup pool — hearts are now earned exclusively via EXTRA
- EXTRA completion grants **+1 golden heart** (6th slot), even if already at 5
- 6th heart slot (`h5`) added to HUD: gold gradient (`#fff0a0` → `#e6a800`), gold stroke, gold drop-shadow when active
- Golden heart positioned at the **leftmost** slot in the gauge
- `_dom.hearts` array and `updateUI` loop extended to handle 6 hearts

### Barrier Tree (build 90)
- New sprout kind (33% spawn chance) — line wall with random fixed angle
- Grows longer each level (`level × 35`px growing → 200px full wall, ~10s lifetime)
- Blocks all **enemy bullets** and **all cannon movement** (player, enemies, bosses) at every level including while growing
- Brown color palette: `#6b3d1a` growing stroke, `#a0622a` wall, `#f0d0a0` bright core
- Max active sprouts raised **5 → 8**

### Ice Turret Nerf (build 90)
- Fire interval **700ms → 840ms** (−20% fire rate)
- Ice bullets no longer interact with the boss

### Misc (build 90)
- `publish.sh` renamed to `deploy.sh`

---

## [2026-03-16] Barrier Tree, EXTRA System & Ice Turret Nerf (build 90)

### New Tree Type — Barrier Wall
- Sprouts now spawn as one of three kinds: **Aegis** (33%), **Miasma** (33%), **Barrier** (33%)
- Barrier sprout spawns with a random fixed angle; drawn as a brown line that grows longer each level (`level × 35`px)
- Touch 5× to activate the full **Barrier Wall** (200px long, ~10s lifetime)
- Blocks **all enemy bullets** on contact (line-segment distance check)
- Blocks movement of **all entities** — enemies, bosses, and the player — perpendicular pushback at every level, even while still growing
- Visual: brown glow (`#8b5a2b`), main wall `#a0622a`, warm cream core `#f0d0a0`, brown timer bar
- Max active sprouts raised: **5 → 8**

### New System — EXTRA Letters
- One of the letters **E · X · T · R · A** randomly drops on the ground every 22s
- Only uncollected letters spawn; no duplicates on the ground at once
- Player touches a letter → it lights up gold on the **EXTRA board** (top-left, beside the menu button)
- Collect all 5 → all enemies and bosses explode (with kill bonuses), player gains **+1 heart**, arena-wide shockwave
- Letters reset to dark after completion and can be collected again
- Board shows dim gray letters until lit; lit letters glow gold with `box-shadow`

### Balance — Ice Turret
- Fire interval increased **700ms → 840ms** (−20% fire rate)
- Ice turret bullets no longer affect the **boss** (skip boss collision entirely)

### Misc
- `publish.sh` renamed to `deploy.sh`

---

## [2026-03-14] Boss Scaling, Miasma Tree & Minimee Spice Product (builds 87–89)

### Boss — Burst Bullet Scaling (build 89)
- Boss bullet count per burst now scales with wave: W5→3, W10→4, W15→5, W20→6, …
- Formula: `Math.floor(wave/5) + 2`
- Bullets remain evenly spread (18° gap each) centered on the player

### New Tree Type — Miasma Tree (build 88)
- Each sprout now randomly spawns as either **Aegis** (green) or **Miasma** (violet) kind — color hints at type while growing
- Touch a miasma sprout 5× to awaken it into a **Miasma Tree**
- Active Miasma Tree emits a pulsing violet aura (radius 130); enemies inside take 1 HP damage every 30 frames
- Kills inside the aura award full score (`100 × wave`)
- Lasts ~9.6s (same as AegisTree); expires with violet particle burst
- Visual: dead/rotted dark trunk, 5 drifting violet cloud puffs, dashed violet circle, purple timer bar

### Minimee → Spice Product (build 87)
- Minimee removed from the pickup drop pool
- Added as spice product **[N]** (key `2` / cycle with `1`/`E`)
- HUD product row now shows `[M] [N] [I]`
- Spending spice spawns a Minimee companion (cap 3); if already at 3, spice is refunded
- All Minimee AI/draw/lifetime logic unchanged — only acquisition method changed

---

## [2026-03-14] Spice Product Cleanup & Bomb Rework (build 86)

### Removed Spice Products
- **Tar Trap [T]**, **Blind Trap [B]**, and **Confuse Trap [C]** removed from the game
- HUD product selector now shows only `[M] [I]`; `1`/`E` cycles between Mine and Ice Turret

### Bomb Pickup — New Behavior
- Bomb no longer kills enemies for score
- Enemies in blast radius (700u) are converted to **spice diamonds** at their position
- Each diamond worth `wave` spice — scales with current wave
- Feed message: `BOMB! N ENEMIES → SPICE ×wave`

### Code Cleanup
- Removed all dead state: `tars[]`, `blindTraps[]`, `confuseTraps[]` arrays, associated constants, draw functions, and AI blocks
- Removed dead `mine.type` field from mine objects
- Fixed indentation bug in enemy AI forEach loop (`const spdMul` was at column 0)
- `getSpiceCost()` now cached per while-loop iteration (was called 3×)
- Mine blast kill bonus cached to avoid double `getWave()` call on same line

---

## [2026-03-12] New Spice Products — Blind Trap & Confuse Trap (build 84)

### New Spice Product — Blind Trap [B]
- Places an orange jagged-ring trap at player position (radius 44, ~10s duration)
- Enemies that enter the radius are **blinded** for 10s: their velocity locks in the current direction (no steering toward player) and they **cannot fire**
- Blinded overlay: orange tint with ✕✕ eyes and an orange countdown bar above the HP bar
- "SIGHT BACK!" float text when the effect expires

### New Spice Product — Confuse Trap [C]
- Places a red jagged-ring trap at player position (radius 44, ~10s duration)
- Enemies that enter the radius are **confused** for 10s: they move normally but **fire player-type bullets toward the nearest other enemy** (friendly fire) instead of the player
- Confused overlay: red tint with `?` swirl symbol and a red countdown bar
- "SNAPPED OUT!" float text when the effect expires

### Product Selector
- HUD row now shows `[M] [T] [I] [B] [C]` — two new buttons added
- `1` / `E` cycle now covers all five products

---

## [2026-03-11] Polish & Balance (build 73 → 82)

### Bug Fix — Mobile Grid Alignment (build 80)
- `drawGrid()` moved inside the zoom transform so the grid scrolls and scales in sync with all world objects on mobile

### Balance — Ice Turret (build 81 → 82)
- Turret is now **invincible** — enemy bullets pass through it
- Lifetime reduced to 10s (was 30s); auto-disappears, no HP system
- Fire rate: 1500ms → 700ms
- Ice bullet speed: 9 → 14, range: 400 → 600
- HP bar removed from draw; only a life bar shown

### Balance — Mine AoE (build 78)
- Mine explosion now destroys **all enemies within radius 200**, not just the trigger enemy
- Kill count shown in feed: `MINE TRIGGERED! N ENEMIES DESTROYED`

### UI — Kill Feed Repositioned (build 77)
- Kill feed moved from top-right to **top-left** (below hamburger, `top: 68px, left: 18px`)
- Left-aligned, subtle white background pill for readability

### Balance — Continue-After-Ad Reset (build 76)
- Upgrade penalty changed from `level - 1` → `floor(level × 2/3)` (min 1)
- `spiceProductCount` also reduced by 2/3 on continue

### Balance — Spice (build 75)
- Spice pickup amount: `random(1–10)` → `random(1 ~ wave+10)` (scales with wave)
- Product cost: starts at 50, +10% per placement (`getSpiceCost()`)
- `/cost` cap in HUD updates live; fill bar relative to current cost

### Product Selector (build 73–74)
- `[M] [T] [I]` HUD buttons replace `[A] [B] [C]`
- `1` key (and `E`) cycles products

---

## [2026-03-11] Spice Products B & C (build 72 → 73)

### New Spice Product — Tar Trap [B]
- Places a sticky tar puddle at player position (radius 48, ~15s duration)
- Enemies inside move at 25% speed — does not kill, purely a slow
- Drawn as a radial brown puddle with an animated ripple ring and "TAR" label
- Fades as timer depletes

### New Spice Product — Ice Turret [C]
- Places a stationary icy cannon at player position (20s lifetime, 5 HP)
- Fires ice bullets every 1.5s at the nearest unfrozen enemy
- Ice bullet hits enemy → **FROZEN** for 5 seconds: can't move or fire
- Frozen enemies show a blue ice-crystal overlay with a cyan freeze timer bar
- "THAWED" float text when freeze expires
- Enemy bullets can damage and destroy the turret (HP bar + life bar displayed)

### Product Switching
- `E` key cycles A → B → C → A
- HUD selector buttons `[A] [B] [C]` appear under the spice bar — tap to switch (mobile-friendly)
- Active product highlighted in amber

---

## [2026-03-11] Spice System (build 71 → 72)

### New Feature — Spice
- Gold diamond pickups appear randomly on the world map every 10s (max 6 at once), each worth 1–10 spice
- Player collects spice by touching; accumulates up to 100 and beyond
- Spice HUD bar displayed under the heart gauge (amber bar, Orbitron label, `/100` cap)

### New Feature — Mine [spice_product_a]
- When spice ≥ 100: one mine is auto-placed at the player's position and 100 spice is deducted (multiple mines if 200+, etc.)
- Mine drawn as a dark metallic disc with orange X and red center dot, labeled "MINE"
- Any enemy that touches a mine: dies instantly (full kill score), mine explodes with shockwave and disappears
- Architecture: `spiceProductType = 'a'` variable ready for future product variants (b, c…)

---

## [2026-03-11] Homing Damage Cap (build 70 → 71)

### Balance — Homing (ARW)
- Homing bullet no longer one-shots enemies (`e.maxHp` damage removed)
- Homing bullet damage capped at `getBD() × 2` — scales with player PWR level

---

## [2026-03-11] Minimee Nerf & Balance (build 69 → 70)

### Balance — Minimee (MIN)
- Minimee now expires 15 seconds after spawn (explodes with feed message "MINIMEE EXPIRED")
- Minimee bullet speed halved vs. player (`getBS() / 2`)
- Minimee fire rate halved vs. player (fire interval doubled: `getFI() * 2`)
- Minimee damage halved vs. player (`getBD() / 2`, min 1)
- Countdown timer displayed above Minimee HP bar; turns red in the last 3 seconds

### Balance — AegisTree
- Tree shield radius increased: 115 → 130

### Balance — Bullet Size (SIZ)
- Per-level size step reduced: `2 → 1.5` (max radius at LV10: ~18.5 vs. 23 before)

---

## [2026-03-11] Balance & AegisTree Polish (build 62 → 69)

### Balance — Enemy
- Enemy fire rate ramp-up slowed: scaling factor `3 → 1.3` (reaches min interval much later)

### Balance — Bullet Size (SIZ)
- Per-level size multiplier reduced: `3 → 2` (−33% growth per level)
- SIZ max level raised: `6 → 10`

### AegisTree (formerly Sprout)
- Grow steps to become a tree increased: `4 → 5` (touch 5 times)
- Touch cooldown between grows doubled: `1s → 2s`
- Float text renamed: `Sprout N/4` → `AegisTree N/5`
- Max level stored as `SPROUT_MAX_LEVEL` constant (no more hardcoded `/5`)

### Bug Fix
- Float display was still showing `/4` after max-step change — corrected to use `SPROUT_MAX_LEVEL`

---

## [2026-03-10] Sprout & Tree Limit (build 61)

### Balance
- Max combined sprouts + trees reduced 10 → 5

---

## [2026-03-10] Sprout Totem, Performance & Balance (build 55 → 60)

### New World Object — Sprout / Tree Totem
- Sprouts spawn in the arena every 18s (max 10 total sprouts + trees)
- Touch a sprout to grow it +1 level (4 touches over 4 seconds, 1s cooldown between each)
- At level 4 the sprout becomes a Tree: blocks all enemy bullets within 115 world-unit radius for 9.6s
- Enemies are physically pushed back at the shield boundary and cannot fire while adjacent
- Trees expire with a particle burst and feed message

### Balance
- Minimee HP: 10 → 5
- Tree shield duration: 8s → 9.6s (+20%)
- Tree shield radius: 100 → 115 (+15%)
- Pickup morph now uses biased pool (3× weight on lowest-level upgrade)
- Spawn pool bias kept at 1× (morph only for the stronger bias)

### Performance Optimizations
- DOM elements cached at startup; updateUI only re-renders on value change
- `zoom` cached on resize instead of recalculated every frame
- Vignette pre-rendered to offscreen canvas on resize
- Grid lines and dots batched into single path calls (10× fewer draw calls)
- Particle shadow blur removed
- Bullet shadow blur reduced (r×2.5 → r×1.2)
- Character cannon shadow blur reduced (10 → 6)

### Low-Spec Auto Mode
- `lowSpec` flag auto-enables when FPS < 40, disables when FPS > 80
- In low-spec mode: skips outer glow gradients, shadow blurs on bullets/chars/pickups/shockwaves/tree/crosshair
- FPS display shows ⚡ indicator when low-spec mode is active

### Bug Fix
- Fixed TDZ crash: `isMobile` declaration moved before `resize()` call

---

## [2026-03-09] New Pickups, Balance & Mobile Polish (build 50 → 54)

### New Pickup — Minimee (MIN, red/orange)
- Companion sub-cannon that orbits the player at 0.8× move speed
- Starts with 10 HP; takes damage from enemy bullets (invincibility frames on hit)
- Fires automatically at the nearest enemy using the same bullet stats as the player
- Max 3 companions active at once; disappears when HP reaches 0
- Colored red/orange to match the player cannon

### New Pickup — Homing (ARW, cyan)
- Activates randomly per shot — rate follows same formula as Pierce: LV1=1%, LV10=19%
- On activation: fires a cyan arrowhead bullet aimed directly at the nearest enemy
- Bullet homes in (steers toward nearest enemy each frame) and one-shots any enemy
- Drawn as a pointed arrowhead shape with cyan glow
- ARW stat added to upgrade status panel

### Balance
- PTS pickup bonus changed to `100 × wave × random(1–10)` (was flat `100 × wave`)
- Pickup spawn pool biased: lowest-level upgrade pickup added twice, increasing its odds
- Bullet Size (SIZ) max level reduced 8 → 6
- Pickup spawn interval reduced 15 000 ms → 14 000 ms

### Visual
- Heart pickup color changed to bright magenta-pink (`#ff1aff`)
- Pickup labels now rendered in Orbitron 8px bold with dark shadow (matches upgrade panel)

### Mobile — Ad Overlay Fix
- Ad overlay changed to `overflow-y: auto` + `align-items: flex-start` so card scrolls instead of being clipped on small screens
- Ad container min-height reduced: 400 px → 280 px desktop, 200 px portrait

---

## [2026-03-09] Mobile Controls & Visual Polish (build 46 → 49)

### Mobile — Virtual Joystick Controls
- Portrait mode (`height > width`) shows transparent joystick strip at bottom of screen
- Left joystick: moves player body (replaces WASD/arrow keys)
- Right joystick: rotates cannon (replaces mouse aim)
- Multi-touch supported: each finger tracked independently by `touch.identifier`
- Viewport meta tag added (`user-scalable=no`) to prevent pinch-zoom interference
- `touchstart`/`touchmove` preventDefault on canvas to block scroll/zoom

### Mobile — World Zoom
- All world-space objects (enemies, bullets, pickups, player, HP bars, particles) scale via `ctx.scale(zoom)` transform centered on screen-center
- `zoom = max(1.5, 700/W)` on portrait — e.g. ~1.8× on a 390px-wide phone
- Desktop unaffected (`zoom = 1`)
- Screen-space UI (crosshair, vignette, hit flash) drawn after transform restore — always full size

### UI — Upgrade Status Panel
- Lifted to `bottom: 194px` on portrait orientation so it clears the joystick strip

### Visual — Crosshair
- Arms lengthened 16 → 26px, lineWidth 1.5 → 2.5, dashes removed
- White glow layer behind orange arms for contrast on any background
- Ring radius 7 → 12px
- Center dot 2.5 → 4px, white fill with orange glow

---

## [2026-03-08] Ad & Balance Tweaks (build 44 → 45)

### Ads — Continue After Ad
- Replaced in-article/fluid AdSense unit with autorelaxed format (slot `4131080299`)

### Gameplay — Continue After Ad
- Upgrade penalty changed from "halve all levels" to "reduce each level by 1" (min 1)

### Balance — Enemy Bullet Speed
- Base speed reduced 10%: `3.04 → 2.74`
- Scaling coefficient reduced 10%: `0.015 → 0.0135`

---

## [2026-03-07] Score Submission Token Fix & AdSense Slot (build 41 → 43)

### Bug Fix — Score Submission
- Fixed `submitLeaderboard()` — frontend was never updated to call `GET ?action=token` before POSTing; now correctly fetches a one-time `{nonce, token}` pair and includes it in the POST body to match the build-40 HMAC backend

### Ads — AdSense Slot
- Replaced placeholder ad slot ID with real slot ID `2883778932` (in-article / fluid format)
- Added `min-height` and `text-align: center` to ad `<ins>` element for proper rendering

---

## [2026-03-07] Security Hardening (build 40)

### Backend — leaderboard/api.php
- Added HMAC token system: server issues a one-time `{nonce, token}` via `GET ?action=token`; POST submissions must include valid nonce+token signed with `MAGIC_KEY`
- Strict format validation on nonce (32 hex chars) and token (64 hex chars) before any processing — prevents DoS via oversized inputs
- Replay protection: used nonces stored in `used_tokens` table; each token accepted only once
- Nickname raw input capped at 80 bytes before sanitization to prevent regex DoS
- `sanitize_input()` helper: strips non-printable/unicode chars, collapses whitespace
- Rate limiting: max 5 score submissions per IP per minute
- DB error messages no longer leak internal file paths

### Backend — leaderboard/config.php
- `CLEAR_SECRET_KEY` rotated from default `changeme` to a strong random key
- Added `MAGIC_KEY` constant for HMAC signing

---

## [2026-03-07] Continue After Ad & AdSense Integration (build 38 → 39)

### Gameplay — Continue After Ad
- Added "CONTINUE AFTER AD" button on game over screen (blue, one-time use per session)
- On click: shows full-screen ad overlay with 5-second countdown before CONTINUE activates
- After ad: resumes game with all upgrade levels halved (min 1), wave halved, score halved, 3 hearts restored
- Button hidden after use — cannot continue twice in the same session
- On full RESTART: continue button resets and becomes available again

### Ads — Google AdSense
- AdSense script added (`ca-pub-2904828185240062`)
- Ad `<ins>` element recreated dynamically on each game over so AdSense reloads the ad every time
- Ad panel: full-width responsive format, enlarged card (max-width 680px, min-height 400px)

---

## [2026-03-07] New Pickups, Enemy Speed Variance & HUD Polish (build 36 → 37)

### Gameplay — New Pickups
- **Pierce (PRC, black)** — fires a special bullet that passes through all enemies on its path; bullet does not disappear on hit
  - Activation rate: 1% base, +2% per level (LV1=1%, LV2=3%, … LV10=19%)
  - Pierce bullet is 1.5× larger than normal, drawn black with white glow
- **Bomb (BOM, yellow)** — instant-use pickup; on touch, destroys all enemies and enemy bullets within 700 world units
  - Scores 100×wave per enemy killed
  - Shockwave ring expands outward on detonation
  - No level progression — purely consumable

### Gameplay — Enemy Speed Variance
- Each enemy now spawns with a random personal speed multiplier (±10% of base)
- Enemies spread out naturally instead of moving in lockstep

### HUD — Upgrade Status Panel (bottom-right)
- Added persistent panel showing current level of all 6 upgrades: MOV, SIZ, VEL, RTE, PWR, PRC
- Each stat displayed as colored `+N` with matching pickup color
- Build 37: upsized panel — larger fonts (label 0.58rem, value 0.9rem), wider spacing

### Balance
- Bullet Size (SIZ) max level reduced from 10 → 8

### Visual
- Pickup icon radius increased 10% (11 → 12)

---

## [2026-03-06] Pickup Morphing & Awakened Tuning (build 35)

### Gameplay — Pickup Morphing
- Pickups now cycle to a random new type every 5 seconds
- Visual flash burst ring effect plays on each morph transition

### Gameplay — Awakened Enemy Tuning
- Awakened threshold increased from 30s → 60s (more time before enemies go berserk)

---

## [2026-03-06] Anti-Cheese & Awakened Enemies (build 29 → 34)

### HUD
- Merged POINT + TOTAL into a single `X,XXX pts` display (totalPoints only)
- HUD layout changed to vertical: score on top, divider, hearts below

### Leaderboard
- Added `clear.php` — secret-key-protected DELETE endpoint
- Added `config.php` — centralized secret key (`CLEAR_SECRET_KEY`)
- `.htaccess` updated to block direct access to `config.php`
- `publish.sh` updated to include `clear.php` and `config.php`
- Added Leaderboard link to hamburger menu

### Gameplay — Awakened Enemies
- Enemies alive for 30+ seconds enter **Awakened** state:
  - Move speed ×1.7
  - Bullet speed ×1.2, bullet size ×1.5 (orange-red color)
  - Visual: spiky rotating ring with red glow, body turns red/orange
  - "AWAKENED!" float text on transformation
- Enemies now wait 1 second after spawning before firing
- Float text font changed to Orbitron

### Anti-Cheese — Directional Spawning
- Enemies now spawn in a ±90° cone ahead of the player's movement direction
- Smoothed move vector tracks player input each frame
- Standing still falls back to fully random spawn angle

---

## [2026-03-06] Pickup-Only Upgrade System (build 20 → 28)

### Gameplay — Pickup System
- Added world-space pickup orbs spawning every 15s near the player (max 7 on map, capped to arena boundary)
- 7 pickup types, each gives a free instant upgrade on touch:
  - Red — Move Speed
  - Blue — Blt.Size
  - Orange — Blt.Speed
  - Violet — Fire Rate
  - Gray — Blt.Power
  - Pink (♥) — +1 Heart (capped at 5; shows "FULL" if already max)
  - Green (PTS) — +100 × wave Points (added to both Point and Total)
- All upgrade types capped at LV 10
- On collection: colored text floats up from pickup position and fades out (20px bold dark gray, no decoration)
- Pickup explodes with particles on collection

### Design Change — Removed Point-Spend Upgrade System
- Removed upgrade panel (buttons) and keyboard shortcuts (U/I/O/P/Y)
- Removed `upgrade()`, `getUpgCost()`, `handleKey()` functions
- Score (Point/Total) is now purely for leaderboard — no longer spent on upgrades
- About modal controls updated to reflect pickup-only upgrade mechanic

### Enemy Tuning
- Reduced enemy fire rate scaling: coefficient `6 → 3` (reaches minimum rate twice as slowly)

---

## [2026-03-05] Anti-cheese Arena & Crash Fix (build 18 → 19)

### Gameplay
- Added circular arena boundary (radius 2200) — player is hard-clamped at the edge, preventing infinite directional movement cheese
- Arena boundary drawn as a glowing orange dashed ring on the grid

### Bug Fix
- Fixed crash `Cannot read properties of undefined (reading 'x')` — when the last life was lost with multiple enemy bullets active, `triggerGameOver()` cleared the `eBullets` array mid-loop causing undefined access; fixed with `break` after game over

---

## [2026-03-05] Bug Fixes & Polish (build 17)

### Bug Fixes
- Fixed nickname input box not available on 2nd+ game over — submit button was left `disabled` after a successful submission and never re-enabled on restart
- Fixed "WAVE 1 — Difficulty increased!" false alarm firing immediately on game start — `lastWave` now initialises to `1`
- Fixed HUD score values displaying raw integers — now formatted with `toLocaleString()` (e.g. `12,345`) to match leaderboard display
- Fixed frozen bullets/enemies/particles rendering behind the game over overlay — arrays are now cleared on game over
- Removed orphaned CSS classes (`.upg-head`, `.upg-icon`, `.upg-key`) and added missing `.upg-title` rule

### Visual
- Crosshair recoloured to game-orange (`#ff6b1a`) with outer ring, thicker dashed arms, glowing center dot — much more visible on the light background

---

## [2026-03-04] UX Fixes (build 16)

### Game Over / Leaderboard
- RESTART button is now always visible on the game over screen (no longer hidden until after score submit)
- Player nickname is saved to `localStorage` on successful submit and auto-filled on the next game over

---

## [2026-03-04] Leaderboard, Score Split & HUD Rework (build 6 → 15)

### Score System
- Split score into two values:
  - **Point** — spendable balance (decremented on upgrades, used in upgrade panel)
  - **Total** — cumulative points ever earned (never decremented, used for leaderboard ranking)
- Both increment together on kills (+100×wave for kill, +20 for hit, +wave/sec passive)

### HUD Rework
- Merged hearts and scores into a single **horizontal strip panel** (top-right)
  - Hearts on the left, vertical divider, POINT + TOTAL labels with values on the right
- Panel background set to **transparent**
- Kill feed repositioned to sit cleanly below the panel

### Leaderboard Sub-project (`leaderboard/`)
- New **PHP + SQLite3** leaderboard backend
  - `api.php` — JSON API: `GET` returns top 20 scores, `POST {name, score, wave}` inserts and returns rank + full scores list
  - `index.php` — standalone leaderboard page with card-style design, gold/silver/bronze top-3 styling
  - `scores.db` — SQLite3 DB, auto-created on first request
  - `.htaccess` — blocks direct HTTP access to `scores.db`
- **Game over overlay** redesigned:
  - Shows Total Points + Wave Reached
  - Leaderboard fetches and renders automatically on game over
  - Name input → Submit Score → highlights player's rank in the list
  - "VIEW FULL LEADERBOARD →" link
  - Removed Skip button

### Bug Fixes
- Fixed `DEFAULT (datetime("now"))` SQLite schema error on PHP 7.4 (old SQLite bundled version doesn't support function-call column defaults) — moved `datetime("now")` into the `INSERT` statement instead
- Fixed `submitLeaderboard is not defined` — caused by browser caching old `game.js`; added `?v=N` cache-busting query string to CSS and JS links, auto-bumped by `publish.sh` on every build
- Fixed "GAME OVER" and "LEADERBOARD" titles line-wrapping on narrow screens — added `white-space: nowrap` and `clamp()` font sizing

### publish.sh
- Now also uploads `leaderboard/api.php`, `leaderboard/index.php`, `leaderboard/.htaccess`
- Auto-updates `?v=N` cache-busting version on `style.css` and `game.js` links in sync with build number

---

## [2026-03-03] Gameplay & UI Overhaul (build 2 → 5)

### Branding
- Added site title **EmptyBoard.com** + build number at top-center
- Build number now auto-increments on every `publish.sh` run

### Difficulty & Scoring
- Auto score: +wave pts every second (passive income)
- Upgrade costs now scale +15% per level (`1000 × 1.15^(lv-1)`)
- Enemy bullet speed at startup reduced to 80% (3.8 → 3.04)

### New Upgrade: Bullet Power `[p]`
- Each level adds +1 damage per hit (LV1=1, LV2=2, …)
- Same cost scaling as other upgrades

### Upgrade Key Map (final)
- `[u]` Blt.Size · `[i]` Blt.Speed · `[o]` Blt.Rate · `[p]` Blt.Power · `[y]` Move Speed

### Upgrade Panel UI
- Replaced long labels with abbreviated text: Blt.Size, Blt.Speed, Blt.Rate, Blt.Power, Move Speed
- Reordered panel: Size → Speed → Rate → Power → Move Speed
- Cost display on buttons updates live each frame

### Wave Display
- Wave label and number now show on a single line ("Wave 1")

---

## [2026-03-03] Delta Time / Frame-Rate Fix

### Changes
- **game.js**: Introduced `dtf` (normalized delta time, `dt / 16.667`) so all
  physics and movement run at consistent speed regardless of frame rate.
  - Camera movement scaled by `dtf`
  - Enemy AI acceleration, friction, position, and bob animation scaled by `dtf`
  - Player and enemy bullets position and lifetime scaled by `dtf`
  - Invincibility timer and hit flash scaled by `dtf`
  - Friction now uses `Math.pow(factor, dtf)` for correct exponential decay
- **index.html**: Updated GitHub link to point to the actual repo
  (`https://github.com/jadumate/canon-game/`)

### Deploy
- Committed and pushed to `master` on GitHub via token in `.env`
